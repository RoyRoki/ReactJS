shoppino is smart shopping app

=> auth/signup (POST)
            {
                "username":"userx",
                "email":"userx@gmail.com",
                "password":"root"
            }

=> auth/login  (POST)
            {
              "email":"userx@gmail.com",
              "password":"root"
            }

